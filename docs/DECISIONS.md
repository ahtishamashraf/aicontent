# Decisions
- Identity calibration v1; public score is not called a probability.
- 384-token windows with 64-token overlap balance CPU memory and context.
- Guest content expires within 24 hours; plaintext tokens are never stored.
- Fake inference is permitted only in non-production test environments.
